# Hugo build output — rebuilt by GitHub Actions on deploy
public/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# MCP server config (contains API keys, never commit)
.mcp.json

# Hugo temp files
.hugo_build.lock
resources/_gen/

# Node / Playwright
node_modules/
test-results/
